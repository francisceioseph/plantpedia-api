<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="/">PlantPedia</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarTogglerDemo02" aria-controls="navbarTogglerDemo02" aria-expanded="false" aria-label="Alterna navegação">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarTogglerDemo02">
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item <%= 'active' if current_page?(plants_path) %>">
        <a class="nav-link" href="/plants">
          <%= t :plants%>
        </a>
      </li>
      <li class="nav-item <%= 'active' if current_page?(species_index_path) %>">
        <a class="nav-link" href="/species">
          <%= t :species%>
        </a>
      </li>
    </ul>

    <form class="form-inline my-2 my-lg-0">
      <% if current_user.present? %>
        <span class="navbar-text mr-3"><%= t :welcome%>, <%= current_user.email%></span>
        <%= link_to t(:logout), destroy_user_session_path, method: :delete%>
      <%end%>
    </form>
  </div>
</nav>