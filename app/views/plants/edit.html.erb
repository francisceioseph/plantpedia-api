<div class="card">
    <div class="card-header">
        <h3>Editing Plant</h3>
    </div>
    <div class="card-body">
        <%= render 'form', plant: @plant %>
    </div>
</div>

<div class="card mt-3">
    <footer class="card-footer">
        <%= link_to t(:show), @plant, class: 'mr-3'%>
        <%= link_to t(:back), plants_path, class: 'mr-3'%>
    </div>
</div>